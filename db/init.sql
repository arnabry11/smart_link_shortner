-- Database initialization script for development
-- This file runs when the PostgreSQL container starts for the first time

-- Create the development database (Rails will handle this, but this ensures it exists)
-- SELECT 'CREATE DATABASE smart_link_shortner_development' WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'smart_link_shortner_development')\gexec
